# DataAcuity Maps - Scheduled Tasks
# Add these to crontab: crontab -e

# Update South Africa tiles monthly (1st of month at 3am)
0 3 1 * * /home/geektrading/maps/scripts/update-tiles.sh >> /var/log/maps-tiles-update.log 2>&1

# Clean up old prerender cache weekly (Sundays at 4am)
0 4 * * 0 docker exec maps_prerender sh -c 'rm -rf /tmp/prerender/*' 2>/dev/null

# Database vacuum (weekly on Saturdays at 2am)
0 2 * * 6 docker exec maps_db psql -U maps -d maps -c "VACUUM ANALYZE;" >> /var/log/maps-vacuum.log 2>&1
