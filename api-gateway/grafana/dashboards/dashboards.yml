apiVersion: 1

providers:
  - name: 'API Gateway Dashboards'
    orgId: 1
    folder: 'API Gateway'
    folderUid: 'api-gateway'
    type: file
    disableDeletion: false
    editable: true
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/provisioning/dashboards
